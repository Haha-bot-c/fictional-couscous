using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sphere : MonoBehaviour
{
    [SerializeField] private Vector3 _movmentDerektion;

    private void Update()
    {
        transform.Translate(_movmentDerektion, Space.World);
    }
}
